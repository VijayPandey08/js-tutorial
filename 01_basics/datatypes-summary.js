// primitive
// call by value
// types: string, number, boolean, null,undefined, symbol, bigint

const score = 100 

// refrence (non-primitive)
// call by refrence
// types : arrays, objects, functions

// java-script is dynamically typed, you do not need to define the data-type

const heros = ["shaktiman","vijay","pandit"]  // array
let myObj = {
    face :"vijay",
    age: 18,
}    //object

const myFunction = function(){
    console.log("hello world");
}
